from app.mktcore.wtfimports 		import *
from app.mktcore.imports 			import *
from app.ScheduleAddon.param 		import *
from app.ScheduleAddon.models 		import *

from app.Category.models 			import *
from app.Transaction.models 		import *

class FRM_SCHEDULE_ADDON_PARAM_EX(FRM_SCHEDULE_ADDON_PARAM):

	# DebitCategory 			= 	QuerySelectField(requiredlabel("Debit Category","*"),
	# 										query_factory=loadCategory,
	# 										get_label=lambda obj:"%s - %s"%(obj.ID,obj.Description),
	# 										allow_blank=True,
	# 										blank_text=u"---Choose Category---",
	# 										validators=[validators.Required()])

	# CreditCategory 			= 	QuerySelectField(requiredlabel("Credit Category","*"),
	# 										query_factory=loadCategory,
	# 										get_label=lambda obj:"%s - %s"%(obj.ID,obj.Description),
	# 										allow_blank=True,
	# 										blank_text=u"---Choose Category---",
	# 										validators=[validators.Required()])

	# PrepaidTran		 		= 	QuerySelectField(requiredlabel("Prepaid Transaction","*"),
	# 										query_factory=loadTransaction,
	# 										get_label=lambda obj:"%s - %s"%(obj.ID,obj.Description),
	# 										allow_blank=True,
	# 										blank_text=u"---Choose Transaction---",
	# 										validators=[validators.Required()])

	# RevPrepaidTran		 	= 	QuerySelectField(requiredlabel("Reverse Prepaid Transaction","*"),
	# 										query_factory=loadTransaction,
	# 										get_label=lambda obj:"%s - %s"%(obj.ID,obj.Description),
	# 										allow_blank=True,
	# 										blank_text=u"---Choose Transaction---",
	# 										validators=[validators.Required()])

	# CollectionOrder 		= 	TextField(requiredlabel("Collection Order","*"),
	# 								validators=[validators.Required()])

	pass