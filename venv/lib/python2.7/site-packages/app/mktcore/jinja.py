#jinja tempalate
def isindex(item,searchdelimiter):
		try:
			item=item[:item.index(searchdelimiter)]
			return True
		except Exception as exe:
			return False
def delimiterdic(item,searchdelimiter):
	keyvalue='1'
	if isindex(item,searchdelimiter):
		keyvalue=(item[item.index(searchdelimiter)+1:])
		#keyvalue.append(item[:item.index(searchdelimiter)])
	return keyvalue

def removestring(item,searchdelimiter):
	keyvalue='1'
	if isindex(item,searchdelimiter):
		keyvalue=(item[item.index(searchdelimiter)+1:])
		#keyvalue=item[:item.index(searchdelimiter)]
	return keyvalue

def contain(item, condition):
	if condition in item:
		return True
	else:
		return False 

def maxValue(fields,searchdelimiter):
	first_value=""
	str1=""
	for f in fields:
		if first_value=="":
			first_value=int(delimiterdic(f.id,searchdelimiter))
		else:
			if first_value<int(delimiterdic(f.id,searchdelimiter)):
				first_value=int(delimiterdic(f.id,searchdelimiter))
	return first_value

