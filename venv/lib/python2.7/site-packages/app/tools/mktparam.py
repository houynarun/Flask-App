from app.mktcore.wtfimports 	import *
from app.mktcore.imports 		import *
from flask 						import flash
from app.TellerParam.models 	import *
from app.PDParameter.models import MKT_PD_PARAM
from app.LoanContract.models import MKT_LOAN_CONTRACT

from sqlalchemy 				import create_engine,inspect

def getTellerParam():
	
	engine 		= create_engine(app.config['SQLALCHEMY_DATABASE_URI'])
	inspector 	= inspect(engine)
	ListTable 	= []
	
	for table_name in inspector.get_table_names():
		ListTable.append(str(table_name))
	
	result=''

	if 'MKT_TELLER_PARAM' in ListTable:

		TellerParam = MKT_TELLER_PARAM.query.get('SYSTEM')

		if TellerParam:

			result = TellerParam
	engine.session.close()
	return result

def getPDParam(LoanProduct="SYSTEM"):
	try:
		if LoanProduct != "SYSTEM":
			PD = MKT_PD_PARAM.query.get(LoanProduct)
		else:
			PD = MKT_PD_PARAM.query.get('SYSTEM')

		if not PD:
			PD = MKT_PD_PARAM.query.get('SYSTEM')
			
		return PD

	except Exception, e:
		return []
