from app.tools.mktmoney import *
from app import ThreadS
def getCurrencyObj(Currency=None,ThreadS=ThreadS):
	try:
		# print 'y1'
		obj = ThreadS.query(MKT_CURRENCY).get('%s' %Currency)
		# print 'y2'
		# ThreadObj.dispose()
		return obj
		
	except Exception as e:
		raise
	# ThreadObj   =   mktthread.ConGuard()
	# ThreadS     =   ThreadObj.ThreadS