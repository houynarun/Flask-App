from app.LoanContract.models 			import *
from app.mktcore.imports 	import *

def loanAmount(recordID):
	amount = 0
	amountObj  = MKT_LOAN_CONTRACT_INAU.query.get(recordID)
	if amountObj:
		amount = amountObj.Disbursed
	return amount