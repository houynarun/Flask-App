from app.mktcore.wtfimports     import *
from .models                    import *
from app.Category.models 		import MKT_CATEGORY


def loadCategory():
	return MKT_CATEGORY.query.order_by(MKT_CATEGORY.ID).all()

# class FRM_ASSET_TYPE
class FRM_ASSET_PAYMENT_ME(exform):
	CategoryID    = QuerySelectField( requiredlabel("Category","*"), 
									query_factory=loadCategory,
									get_label=lambda c: c.ID + "-" + c.Description,
									blank_text=u'--Choose Category--',
									allow_blank=True,								
									validators=[validators.Required()],			
									)
	Description = TextAreaField(requiredlabel('Description','*'), 
								validators=[validators.Required(), 
								validators.Length(max=100)]
								)
	# PaymentType = SelectField(requiredlabel("Payment Type","*"),
	# 							choices=[("","--Choose Payment Type--"),
	# 									("1","Acquisition"),
	# 									("2","Repair / Maintenance"),
	# 									("3","Write Off / Dispose")]
	# 						)